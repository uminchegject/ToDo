●Window
○面積比、有無の値をNoise,randomで設定して検証(GreebleNodeに直接コーディング)
noiseValue = 
cofficientValue = noiseValue + random(@P) * 0.3
if(0.5 < cofficientValue)
{
	greebleBlock.width = width * cofficientValue
	greebleBlock.height = height * cofficientValue
	makeprim
}

○GreebleBlockから均等に分割する

float minWidthSize = ch("MinWidthSize");
float minHeightSize = ch("MinHeightSize");

float sizeX = 
float sizeY = 

int divideXNum = int(randnum(@P) * sizeX / minWidthSize)
int divideYNum = int(randnum(@P * 100)* sizeX / minWidthSize)

float offsetRatio =

float windowXSize = sizeX / divideXNum * offsetRatio; 
float windowYSize = sizeY / divideYNum * offsetRatio;

for(int i = 0; i < divideXNum; ++i)
{
	for(int j = 0; i < divideYNum; ++j)
	{
		//Windowを生成
	}

}

●Facade
○検証用ロジックを考える
