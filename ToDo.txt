bool bEnableScrollScrollBox()

○現在スクロールボックスを選択しているか
FWidgetPath WidgetPath = SlateApp.LocateWindowUnderMouse(Posision, SlateApp.GetInteractiveTopLevelWindows());
if (!WidgetPath.IsValid()) { break; }

const FName SScrollBoxName("SScrollBox");
const FArrangedChildren::FArrangedWidgetArray& AllArrangedWidgets = WidgetPath.Widgets.GetInternalArray();
for (const FArrangedWidget& ArrangedWidget : AllArrangedWidgets)
{
	TSharedRef<SWidget> Widget = ArrangedWidget.Widget;
	if (Widget->GetType() == SScrollBoxName)
	{
		auto ScrollBox = StaticCastSharedRef<SScrollBox>(Widget);
		IsScrollBox = true;
		return &ScrollBox;
	}
}

○次スクロールボックス以外を選択するか

○カーソルの選択先に対してスクロールはまだできるか
if (CursorInput.IsRepeat(ESBCursorInputKey::Gamepad_Up))
{
	if(ScrollBox->IsScrollStart())
	{
		return false;
	}	
}
if (CursorInput.IsRepeat(ESBCursorInputKey::Gamepad_Down)) 
{
	if(ScrollBox->IsScrollEnd())
	{
		return false;
	}
}


if (CursorInput.IsRepeat(ESBCursorInputKey::Gamepad_Left))
{
	if (ScrollBox->GetOrientation() == EOrientation::Orient_Horizontal && ScrollBox->IsScrollEnd())
	{
		return false;
	}
}
if (CursorInput.IsRepeat(ESBCursorInputKey::Gamepad_Right))
{

}

return true;
