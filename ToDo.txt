[Private]
○ScrollBoxからインデックスを取得

・FChildren::GetChildAt()
int ChildrenNum = FChildren::num();

int GetSelectingIndexInScrollBox(SWidget* SelectingWidget), SScrollBox* ScrollBox)
{
	FChildren* ScrollBoxChildren 
	for(int i = 0; i < ScrollBoxChildren->Num(); ++i)
	{
		if(SelectingWidget == GetChildAt(i))
		{
			return i;
		}
	}
}
