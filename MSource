matrix mTrans = ident();
vector dir = chv("TransformVector");
translate(mTrans,dir);

vector normalDir = normalize(dir);
matrix mRotate = dihedral(normalDir, @N);

dir *= mRotate;

@P += dir; // 移動行列を乗算
